spring:
  datasource:
    url: jdbc:postgresql://postgres:5432/order_db
  rabbitmq:
    host: rabbitmq
    port: ${RABBIT_PORT}

eureka:
  client:
    service-url:
      defaultZone: http://eureka-server:8761/eureka/
