spring:
  datasource:
    url: jdbc:postgresql://postgres:5432/delivery_db

eureka:
  client:
    service-url:
      defaultZone: http://eureka-server:8761/eureka/

spring:
  rabbitmq:
    host: rabbitmq
